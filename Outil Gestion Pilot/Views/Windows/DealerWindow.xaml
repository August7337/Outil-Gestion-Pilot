<ui:FluentWindow x:Class="Outil_Gestion_Pilot.Views.Windows.DealerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"

        Title="Revendeur"
        Width="300"
        Height="495"
                 
        d:DesignHeight="495"
        d:DesignWidth="300"
        ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
        ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
        ExtendsContentIntoTitleBar="True"
        Foreground="{DynamicResource TextFillColorPrimaryBrush}"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <StackPanel>
        <ui:TitleBar
            x:Name="TitleBar"
            Title="Revendeur"
            Grid.Row="0"
            CloseWindowByDoubleClickOnIcon="True">
            <ui:TitleBar.Icon>
                <ui:ImageIcon Source="pack://application:,,,/Assets/wpfui-icon-256.png"/>
            </ui:TitleBar.Icon>
        </ui:TitleBar>
        
        <Label Content="_Numéro revendeur" Margin="0,20,0,0" Width="200" Target="{Binding ElementName=box_Num}"/>
        <ui:TextBox x:Name="box_Num" TextWrapping="Wrap" Text="{Binding NumeroRevendeur}" Width="200" Margin="0,0,0,10"/>

        <Label Content="_Raison sociale" Width="200" Target="{Binding ElementName=box_RS}"/>
        <ui:TextBox x:Name="box_RS" TextWrapping="Wrap" Text="{Binding RaisonSociale}" Width="200" Margin="0,0,0,10"/>

        <Label Content="R_ue" Width="200" Target="{Binding ElementName=box_Rue}"/>
        <ui:TextBox x:Name="box_Rue" TextWrapping="Wrap" Text="{Binding Rue}" Width="200" Margin="0,0,0,10"/>

        <Label Content="_Code postal" Width="200" Target="{Binding ElementName=box_CP}"/>
        <ui:TextBox x:Name="box_CP" TextWrapping="Wrap" Text="{Binding Cp}" Width="200" Margin="0,0,0,10"/>

        <Label Content="_Ville" Width="200" Target="{Binding ElementName=box_Ville}"/>
        <ui:TextBox x:Name="box_Ville" TextWrapping="Wrap" Text="{Binding Ville}" Width="200" Margin="0,0,0,10"/>

        <ui:Button x:Name="but_Revendeur" Content="Ajouter revendeur" HorizontalAlignment="Center" Margin="0,15,0,0" Width="150" Appearance="Primary"  Click="but_Revendeur_Click" IsDefault="True"/>
    </StackPanel>
</ui:FluentWindow>
