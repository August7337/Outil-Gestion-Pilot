<Page
    x:Class="Outil_Gestion_Pilot.Views.Pages.CartPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Outil_Gestion_Pilot.Views.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="CartPage"
    d:DataContext="{d:DesignInstance local:CartPage,
                                 IsDesignTimeCreatable=False}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    mc:Ignorable="d">
    <Grid>
        <StackPanel>
            <StackPanel Orientation="Horizontal" Margin="200,0,0,20">
                <StackPanel Margin="0,0,20,0">
                    <Label>Code Produit :</Label>
                    <ui:TextBox 
                        Width="120"
                        PlaceholderText="Entrer un code" 
                        x:Name="searchCodeTxt" 
                        Text="{Binding SearchCode, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>
                <StackPanel Margin="0,0,20,0">
                    <Label>Prix :</Label>
                    <ui:NumberBox MinWidth="100" />
                </StackPanel>
                <StackPanel Margin="0,0,20,0">
                    <Label>Quantité :</Label>
                    <ui:NumberBox MinWidth="100" />
                </StackPanel>
                <!--<StackPanel Margin="0,0,20,0">
                    <Label>Catégorie</Label>
                    <ComboBox
                        SelectedIndex="0" >
                        <ComboBoxItem Content="Toutes" />
                        <ComboBoxItem Content="Bureau" />
                        <ComboBoxItem Content="Loisir" />
                    </ComboBox>
                </StackPanel>
                <StackPanel Margin="0,0,20,0">
                    <Label>Type :</Label>
                    <ComboBox
                        SelectedIndex="0">
                        <ComboBoxItem Content="Tout" />
                        <ComboBoxItem Content="Roller gel" />
                        <ComboBoxItem Content="Couleur fun" />
                        <ComboBoxItem Content="Frixion Ball" />
                    </ComboBox>
                </StackPanel>-->
                
                

            </StackPanel>

            <DataGrid
                x:Name="CartDG"
                AutoGenerateColumns="False"
                IsReadOnly="True"
                ItemsSource="{Binding CartView}">
                <DataGrid.Columns>
                    <DataGridTextColumn
                        Width="*"
                        Binding="{Binding ImagePath}"
                        Header="Image" />
                    <DataGridTextColumn
                        Width="*"
                        Binding="{Binding Code}"
                        Header="Code Produit" />
                    <DataGridTextColumn
                        Width="*"
                        Binding="{Binding Name}"
                        Header="Nom produit" />
                    <DataGridTextColumn
                        Width="*"
                        Binding="{Binding SellingPrice, StringFormat={}{0:N2} €}"
                        Header="Prix vente" />
                    <DataGridTextColumn
                        Width="*"
                        Binding="{Binding Stock}"
                        Header="Quantité stocks" />

                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</Page>